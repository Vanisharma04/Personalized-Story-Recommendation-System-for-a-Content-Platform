# Runtime (serving) dependencies — used by the Streamlit dashboard and the
# deployed app. The serving path is FAISS + LightGBM over precomputed vectors,
# so it needs neither torch nor sentence-transformers (kept light for cloud).
# To retrain the models, also install requirements-train.txt.
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
faiss-cpu>=1.8
lightgbm>=4.3
scikit-learn>=1.4
streamlit>=1.36
plotly>=5.20
